import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from '@/components/providers';

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-inter',
  display: 'swap',
});

export const metadata: Metadata = {
  title: {
    default: 'ROS — Restaurant Operating System',
    template: '%s | ROS',
  },
  description: 'Professional restaurant management platform — POS, Kitchen, Inventory, Analytics and more.',
  keywords: ['restaurant', 'POS', 'kitchen display', 'inventory', 'restaurant management'],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
